entero = 1
while( entero != 0 ) do
	puts( "Input an integer (0 to exit): " )
	entero = geti
	puts( "La entrada ha sido #{entero}\n" )
end

#caracter = 'a'
#while( caracter < 'q' ) do
	puts( "Input an char: " )
	caracter = getc
	puts( "La entrada ha sido #{caracter}\n" )
#end

float = 1.0
while( float > 0.0 ) do
 	puts( "Input an float: " )
	float = getf
	puts( "La entrada ha sido #{float}\n" )
end
